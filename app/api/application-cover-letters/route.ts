import { NextRequest, NextResponse } from 'next/server';
import { z } from 'zod'; import { getSupabaseClient, getDevUserId } from '@/lib/supabase';
const supabase = getSupabaseClient();
const Schema = z.object({ application_id: z.string().min(1), prompt_text: z.string().optional().default(''), raw_response: z.string().optional().default(''), edited_content: z.string().optional().default(''), is_locked: z.boolean().optional().default(false), is_accepted: z.boolean().optional().default(false) });
export async function GET(req:NextRequest){ const {searchParams}=new URL(req.url); const application_id=searchParams.get('application_id'); if(!application_id) return NextResponse.json({data:null,error:'application_id required'},{status:400}); const {data,error}=await supabase.from('application_cover_letters').select('*').eq('application_id',application_id).maybeSingle(); if(error) return NextResponse.json({data:null,error:error.message},{status:500}); return NextResponse.json({data,error:null}); }
export async function POST(req:NextRequest){ try{ const parsed=Schema.safeParse(await req.json()); if(!parsed.success) return NextResponse.json({data:null,error:parsed.error.errors[0]?.message},{status:400}); const row={...parsed.data,user_id:getDevUserId(),accepted_at:parsed.data.is_accepted?new Date().toISOString():null,updated_at:new Date().toISOString()}; const existing=await supabase.from('application_cover_letters').select('*').eq('application_id',row.application_id).maybeSingle(); if(existing.data){ const {data,error}=await supabase.from('application_cover_letters').update(row).eq('id',existing.data.id).select().single(); if(error) return NextResponse.json({data:null,error:error.message},{status:400}); return NextResponse.json({data,error:null}); } const {data,error}=await supabase.from('application_cover_letters').insert(row).select().single(); if(error) return NextResponse.json({data:null,error:error.message},{status:400}); return NextResponse.json({data,error:null},{status:201}); } catch { return NextResponse.json({data:null,error:'Internal server error'},{status:500}); } }
