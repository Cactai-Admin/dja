import { NextRequest, NextResponse } from 'next/server';
import { z } from 'zod';
import { getSupabaseClient, getDevUserId } from '@/lib/supabase';
const supabase = getSupabaseClient();
const Schema = z.object({ application_id: z.string().min(1), keyword: z.string().min(1), category: z.string().optional().default('other'), origin_type: z.string().optional().default('research'), origin_user_keyword_id: z.string().nullable().optional(), is_reused: z.boolean().optional().default(false), is_hidden: z.boolean().optional().default(false), is_selected: z.boolean().optional().default(true), notes: z.string().optional().default('') });
export async function GET(req: NextRequest) { const { searchParams } = new URL(req.url); const application_id = searchParams.get('application_id'); if (!application_id) return NextResponse.json({ data: [], error:'application_id required' }, {status:400}); const { data, error } = await supabase.from('application_keywords').select('*').eq('application_id', application_id).order('updated_at',{ascending:false}); if (error) return NextResponse.json({ data: [], error:error.message }, {status:500}); return NextResponse.json({ data:data ?? [], error:null}); }
export async function POST(req: NextRequest) { try { const body = await req.json(); const items = Array.isArray(body) ? body : [body]; const parsed = z.array(Schema).safeParse(items); if (!parsed.success) return NextResponse.json({ data:null,error:parsed.error.errors[0]?.message }, {status:400}); const rows = parsed.data.map((r)=>({ ...r, user_id:getDevUserId() })); for (const row of rows) { const existing = await supabase.from('application_keywords').select('*').eq('application_id', row.application_id).eq('keyword', row.keyword).maybeSingle(); if (existing.data) { await supabase.from('application_keywords').update({ ...row, updated_at:new Date().toISOString() }).eq('id', existing.data.id); } else { await supabase.from('application_keywords').insert(row); } } const { data, error } = await supabase.from('application_keywords').select('*').eq('application_id', rows[0].application_id).order('updated_at',{ascending:false}); if (error) return NextResponse.json({ data:null,error:error.message }, {status:400}); return NextResponse.json({ data:data ?? [], error:null }); } catch { return NextResponse.json({ data:null,error:'Internal server error' }, {status:500}); } }
