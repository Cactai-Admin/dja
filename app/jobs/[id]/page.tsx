'use client';
import { useEffect, useMemo, useState } from 'react';
import { useParams, useRouter } from 'next/navigation';
import { CheckSquare, Copy, Download, ExternalLink } from 'lucide-react';
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from '@/components/ui/dialog';
import { AutosaveBox } from '@/components/workflow/autosave-box';
import { IconCopyButton } from '@/components/workflow/icon-copy-button';
import type { ApplicationCoverLetter, ApplicationEvidence, PipelineJob } from '@/types';

function downloadText(content: string, filename: string) { const blob = new Blob([content], { type: 'text/plain;charset=utf-8' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = filename; a.click(); URL.revokeObjectURL(url); }

export default function CoverLetterReviewPage() {
  const { id } = useParams<{ id: string }>(); const router = useRouter();
  const [job, setJob] = useState<PipelineJob | null>(null); const [draft, setDraft] = useState<ApplicationCoverLetter | null>(null); const [accepted, setAccepted] = useState(false); const [overviewApplied, setOverviewApplied] = useState(false); const [promptText, setPromptText] = useState('');
  useEffect(() => { (async () => { const jobRes = await fetch(`/api/jobs/${id}`, { cache:'no-store' }); const jobPayload = await jobRes.json(); setJob(jobPayload.data); const appId = jobPayload.data?.application_id; if (!appId) return; const [evidenceRes, coverRes] = await Promise.all([ fetch(`/api/application-evidence?application_id=${appId}`), fetch(`/api/application-cover-letters?application_id=${appId}`) ]); const [evidencePayload, coverPayload] = await Promise.all([evidenceRes.json(), coverRes.json()]); const evidenceSummary = (evidencePayload.data ?? []).map((unit: ApplicationEvidence) => `- ${unit.title}: ${unit.resume_bullet || unit.result}`).join('
'); setPromptText(`Write a focused cover letter for ${jobPayload.data?.title} at ${jobPayload.data?.company}.

Use these evidence units:
${evidenceSummary || '[No evidence saved]'}

Requirements:
- 3 to 4 paragraphs
- tie directly to the role
- use measurable proof points
- stay specific, direct, and credible.`); setDraft(coverPayload.data); setAccepted(coverPayload.data?.is_accepted ?? false); setOverviewApplied(!!jobPayload.data?.application_sent_at); })(); }, [id]);
  async function saveDraft(edited_content: string, is_accepted = accepted) { if (!job?.application_id) return; const res = await fetch('/api/application-cover-letters', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ application_id: job.application_id, prompt_text: promptText, edited_content, raw_response: edited_content, is_accepted }) }); const payload = await res.json(); if (payload.data) setDraft(payload.data); }
  const reviewContent = useMemo(() => draft?.edited_content || draft?.raw_response || '', [draft]); if (!job) return <div className="p-4 text-sm text-muted-foreground">Loading cover letter workspace...</div>;
  return (<div className="space-y-6"><div className="rounded-2xl border border-border bg-card p-4"><div className="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div><p className="text-sm font-semibold text-foreground">Cover letter review and application finish</p><p className="mt-1 text-sm text-muted-foreground">Finalize the cover letter, then open the listing overview to mark the packet complete or application submitted.</p></div><Dialog><DialogTrigger asChild><button type="button" className="inline-flex items-center gap-2 rounded-xl bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground shadow-lg shadow-primary/20 hover:bg-primary/90">Listing overview</button></DialogTrigger><DialogContent className="border-border bg-background text-foreground"><DialogHeader><DialogTitle>{job.title} · {job.company}</DialogTitle></DialogHeader><div className="space-y-4"><div className="rounded-xl border border-border bg-card p-4 text-sm text-muted-foreground"><p><strong className="text-foreground">Pipeline stage:</strong> {job.pipeline_stage}</p><p><strong className="text-foreground">Application stage:</strong> {job.application_stage || 'not set'}</p>{job.url ? <a href={job.url} target="_blank" rel="noreferrer" className="mt-2 inline-flex items-center gap-2 text-primary hover:text-primary/80"><ExternalLink className="h-4 w-4" />Open source listing</a> : null}</div><label className="flex items-center gap-3 rounded-xl border border-border bg-card px-4 py-3 text-sm text-foreground"><input type="checkbox" checked={overviewApplied} onChange={async (e)=>{ const checked=e.target.checked; setOverviewApplied(checked); await fetch(`/api/jobs/${id}`, { method:'PATCH', headers:{'Content-Type':'application/json'}, body: JSON.stringify(checked ? { application_stage:'sent', application_sent_at:new Date().toISOString() } : { application_stage:'completed', application_sent_at:null }) }); }} />Mark as applied / submitted</label><div className="flex flex-wrap gap-2"><button type="button" onClick={async ()=>{ await fetch(`/api/jobs/${id}`, { method:'PATCH', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ application_stage:'completed', application_completed_at:new Date().toISOString() }) }); router.push('/dashboard'); router.refresh(); }} className="rounded-xl border border-border px-4 py-2 text-sm font-medium text-foreground">I'll submit later</button></div></div></DialogContent></Dialog></div></div><Tabs defaultValue="prompt" className="space-y-4"><TabsList className="grid grid-cols-3 rounded-xl border border-border bg-black/20 p-1"><TabsTrigger value="prompt">Prompt</TabsTrigger><TabsTrigger value="draft">Draft</TabsTrigger><TabsTrigger value="review">Review</TabsTrigger></TabsList><TabsContent value="prompt"><div className="flex items-start gap-4 rounded-2xl border border-border bg-card p-4"><IconCopyButton text={promptText} className="h-12 w-12" /><pre className="max-h-96 flex-1 overflow-auto whitespace-pre-wrap text-xs text-muted-foreground">{promptText}</pre></div></TabsContent><TabsContent value="draft"><AutosaveBox value={draft?.edited_content ?? draft?.raw_response ?? ''} onChange={(value)=>setDraft((prev)=>prev ? { ...prev, edited_content:value, raw_response:value } : { id:'', application_id: job.application_id!, user_id:'', prompt_text:promptText, raw_response:value, edited_content:value, is_locked:false, is_accepted:false, accepted_at:null, version:1, created_at:new Date().toISOString(), updated_at:new Date().toISOString() })} onAutosave={(value)=>saveDraft(value)} rows={16} placeholder="Paste or edit the final cover letter here..." /></TabsContent><TabsContent value="review" className="space-y-4"><div className="rounded-2xl border border-border bg-card p-4"><pre className="max-h-[32rem] overflow-auto whitespace-pre-wrap text-sm text-muted-foreground">{reviewContent || 'No cover letter content saved yet.'}</pre></div><label className="flex items-center gap-3 rounded-xl border border-border bg-card px-4 py-3 text-sm text-foreground"><input type="checkbox" checked={accepted} onChange={async (e)=>{ setAccepted(e.target.checked); await saveDraft(draft?.edited_content ?? draft?.raw_response ?? '', e.target.checked); await fetch(`/api/jobs/${id}`, { method:'PATCH', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ application_stage: e.target.checked ? 'completed' : 'cover_letter', application_completed_at: e.target.checked ? new Date().toISOString() : null }) }); }} /><span className="inline-flex items-center gap-2"><CheckSquare className="h-4 w-4 text-primary" />Mark cover letter accepted</span></label><div className="flex flex-wrap gap-2"><button type="button" disabled={!accepted || !reviewContent} onClick={()=>navigator.clipboard.writeText(reviewContent)} className="inline-flex items-center gap-2 rounded-xl border border-border px-4 py-2 text-sm font-medium text-foreground disabled:opacity-40"><Copy className="h-4 w-4" />Copy</button><button type="button" disabled={!accepted || !reviewContent} onClick={()=>downloadText(reviewContent, `${job.company}-${job.title}-cover-letter.md`.replace(/\s+/g,'-').toLowerCase())} className="inline-flex items-center gap-2 rounded-xl border border-border px-4 py-2 text-sm font-medium text-foreground disabled:opacity-40"><Download className="h-4 w-4" />Download</button></div></TabsContent></Tabs></div>);
}
