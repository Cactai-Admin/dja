import Link from 'next/link';
import { Plus, Sparkles, Trophy, Zap } from 'lucide-react';
import { MainLayout } from '@/components/layout/main-layout';
import { JobCard } from '@/components/dashboard/job-card';
import { StatsBar } from '@/components/dashboard/stats-bar';
import { getCurrentWorkflowStage } from '@/lib/pipeline';
import { getRecommendedJobs, getAddedJobs, getVisibleJobs } from '@/lib/jobs';

export default async function DashboardPage() {
  const visibleJobs = await getVisibleJobs();
  const currentPacket = visibleJobs.find((job) => job.pipeline_stage === 'preparing') ?? null;
  const recommended = await getRecommendedJobs();
  const selected = await getInterestedJobs();
  const pipeline = visibleJobs.filter((job) => job.pipeline_stage !== 'new');
  return <MainLayout currentJob={currentPacket}><div className="page-container space-y-8"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><div className="mb-1 flex items-center gap-2"><Zap className="h-5 w-5 text-primary" /><h1 className="text-2xl font-bold text-foreground">Jobs</h1></div><p className="text-sm text-muted-foreground">Stay focused, complete the packet, and submit one job at a time.</p></div><Link href="/jobs/new" className="flex w-full items-center justify-center gap-2 rounded-xl bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground shadow-lg shadow-primary/20 transition-all hover:-translate-y-0.5 hover:bg-primary/90 sm:w-auto"><Plus className="h-4 w-4" />Add Job</Link></div><StatsBar jobs={visibleJobs} />{currentPacket ? <section className="rounded-2xl border border-primary/25 bg-primary/5 p-5 shadow-lg shadow-primary/10"><div className="mb-4 flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-primary/80">Current packet</p><h2 className="mt-2 text-xl font-semibold text-foreground">{currentPacket.title}</h2><p className="mt-1 text-sm text-muted-foreground">{currentPacket.company} · {currentPacket.location}</p></div><Sparkles className="h-5 w-5 text-primary" /></div><p className="text-sm text-muted-foreground">{getCurrentWorkflowStage(currentPacket.application_stage)?.description ?? 'Continue the current packet.'}</p><div className="mt-4 flex flex-col gap-3 sm:flex-row sm:items-center"><Link href={getCurrentWorkflowStage(currentPacket.application_stage)?.href(currentPacket.id) ?? `/jobs/${currentPacket.id}`} className="inline-flex items-center justify-center rounded-xl bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground">Continue current packet</Link><Link href={`/jobs/${currentPacket.id}`} className="inline-flex items-center justify-center rounded-xl border border-border px-4 py-2 text-sm font-medium text-foreground">View job journey</Link></div></section> : <section className="rounded-2xl border border-dashed border-border bg-card/60 p-6 text-center"><Trophy className="mx-auto mb-3 h-8 w-8 text-primary/70" /><h2 className="text-lg font-semibold text-foreground">No current packet in progress</h2><p className="mt-2 text-sm text-muted-foreground">Start the next best job from your selected list and keep momentum moving forward.</p></section>}<section className="space-y-4"><div className="flex items-center justify-between gap-3"><h2 className="text-base font-semibold text-foreground">Pipeline</h2><span className="text-xs text-muted-foreground">{pipeline.length} jobs</span></div><div className="grid grid-cols-1 gap-3 sm:grid-cols-2 xl:grid-cols-3">{pipeline.map((job) => <JobCard key={job.id} job={job} />)}</div></section><section className="space-y-4"><div className="flex items-center justify-between gap-3"><h2 className="text-base font-semibold text-foreground">Selected</h2><span className="text-xs text-muted-foreground">{selected.length} jobs</span></div><div className="grid grid-cols-1 gap-3 sm:grid-cols-2 xl:grid-cols-3">{selected.map((job) => <JobCard key={job.id} job={job} />)}</div></section><section className="space-y-4"><div className="flex items-center justify-between gap-3"><h2 className="text-base font-semibold text-foreground">Recommended</h2><span className="text-xs text-muted-foreground">{recommended.length} jobs</span></div><div className="grid grid-cols-1 gap-3 sm:grid-cols-2 xl:grid-cols-3">{recommended.map((job) => <JobCard key={job.id} job={job} />)}</div></section></div></MainLayout>;
}
